@model IEnumerable<FestiSpec.Domain.Model.Vragenlijst>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Versie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opmerking)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Is_Ingevuld)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Template1.Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Versie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opmerking)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Is_Ingevuld)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Template1.Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
